import { createSlice } from "@reduxjs/toolkit";

export const {actions ,reducer} = createSlice({
    name : "book-order",
    initialState:[],
    reducers:{
        addBookOrder: (state, action)=>{

        },
        removeBookOrder: (state, action)=>{

        },
        updateBookOrder: (state, action)=>{
            
        }
    }
})