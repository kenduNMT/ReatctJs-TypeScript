"use strict";
// Khai báo hai biến với kiểu dữ liệu string và number
let strVar = "2";
let numVar = 2;
// So sánh hai biến và in ra kết quả
// let isEqual: boolean = (strVar == numVar);
// let isStrictEqual: boolean = (strVar === numVar);
// console.log(`Kết quả của so sánh == giữa ${strVar} và ${numVar}: ${isEqual}`);
// console.log(`Kết quả của so sánh === giữa ${strVar} và ${numVar}: ${isStrictEqual}`);
// Gặp lỗi  This comparison appears to be unintentional because the types 'string' and 'number' have no overlap. Nhưng vẫn so sánh được 
